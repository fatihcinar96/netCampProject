@model List<Catalog.DataAccess.Category>
@{
    ViewData["Title"] = "List";
}

<h1>Category List</h1>

<table class="table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Name</th>
        </tr>
    </thead>
    <tbody>

        @foreach (var item in Model)
        {
            int i = 0;
            i++;
        <tr>
            <th scope="row">@i</th>
            <td>@item.Name</td>
            <td><a>@Html.ActionLink("Update", "Update", "Category",new {id = item.CategoryID })</a></td>
            <td><a>@Html.ActionLink("Delete", "Delete", "Category", new { id = item.CategoryID })</a></td>
        </tr>
        }

    </tbody>
    @Html.ActionLink("Create New", "Add", "Category", new { @class = "form-control" })
</table>

